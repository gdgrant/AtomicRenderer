This is a simple rendering engine for digital pointillism, plus a handful of designs and a basic image-generating demo.

To try it out, run `generate_image.py`.  You'll need NumPy and PIL installed in your Python environment.
